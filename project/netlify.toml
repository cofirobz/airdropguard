[build]
  command = "npm run build"
  publish = "dist"

# Serve static asset files directly, then SPA fallback.
[[redirects]]
  from = "/assets/*"
  to = "/assets/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
